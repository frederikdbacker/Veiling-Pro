node_modules
dist
dist-ssr

.env
.env.local
.env.*.local

.vercel

*.log
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code — persoonlijke overrides per Mac (team-instellingen
# zitten in .claude/settings.json en worden wel gecommit).
.claude/settings.local.json
# Opstartconfiguratie is machine-gebonden (poort/pad verschillen per Mac).
.claude/launch.json
